<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.makefulapp.android.makeful" >
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/mf_icon_final"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="com.makefulapp.android.makeful.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.makefulapp.android.makeful.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.makefulapp.android.makeful.InstructionActivity"
            android:label="@string/title_activity_instruction"
            android:parentActivityName="com.makefulapp.android.makeful.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.makefulapp.android.makeful.MainActivity" />
        </activity>
        <activity
            android:name="com.makefulapp.android.makeful.ProjectListActivity"
            android:label="@string/title_activity_project_list"
            android:parentActivityName="com.makefulapp.android.makeful.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.makefulapp.android.makeful.MainActivity" />
        </activity>
    </application>

</manifest>
